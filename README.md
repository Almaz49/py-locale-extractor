# py-locale-extractor

Автоматическая миграция строк с кириллицей в словарь локализации для Telegram-ботов на **aiogram** (и других Python-проектов).

Превращает:
```python
await message.answer(f"Привет, {name}! Ваш баланс: {balance} руб.")
```
в:
```python
await message.answer(
    get_text("greet_user_balance", lang).format(name=name, balance=balance)
)
```
и добавляет в `LEXICON_RU.auto.py`:
```python
"greet_user_balance": "Привет, {name}! Ваш баланс: {balance} руб.",
```

## ✨ Возможности
- Поддержка обычных и f-строк (включая многострочные)
- Автогенерация уникальных ключей через замену русских слов на их перевод из `RU_TO_EN.py` или транслитерацию
- Сохранение контекста: запятые, скобки, другие аргументы не теряются
- Резервная копия исходного файла
- Работает с модульной структурой хэндлеров

## 🚀 Как использовать
1. Скачайте `refactor_lexicon.py` и `RU_TO_EN.py` либо сделайте свой словарь
2. Настройте константы в начале скрипта:
   ```python
   INPUT_FILE = "handlers/my_handler.py"
   LEXICON_OUTPUT_FILE = "LEXICON_RU.auto.py"
   ```
3. Запустите:
   ```bash
   python refactor_lexicon.py
   ```
4. Проверьте результат и вручную объедините составные строки, если нужно.

## ⚠️ Важно
- Скрипт заменяет **только строки с кириллицей**
- f-строки с простыми переменными (`{name}`) обрабатываются автоматически
- Сложные выражения (`{user.name.upper()}`) помечаются как `NEEDS_FIX`
- Многострочные блоки надо потом править вручную
- Не обрабатываются текстовые строки в логах и описаниях фунций.
- Обрабатываются тексты только в тех функциях, список которых указан в SEND_FUNCTIONS

## 📄 Лицензия
MIT — свободно используйте, модифицируйте и распространяйте.
```
